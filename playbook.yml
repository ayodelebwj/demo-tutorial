- name: Create folder in Ubuntu home
  hosts: all
  become: true

  tasks:
    - name: Create directory
      file:
        path: /home/ubuntu/myfolder
        state: directory
        owner: ubuntu
        group: ubuntu
        mode: '0755'